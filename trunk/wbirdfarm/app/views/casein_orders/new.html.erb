<!-- Scaffolding generated by Casein v.2.0.6 -->

<h2>Add a new order</h2>

<% form_for :order, @order, :url => casein_orders_path do |form| %>
	
	<%= render :partial => 'form', :locals => {:form => form, :order => @order} %>
	
	<p class="submits">
		<a href="<%= url_for casein_orders_path %>" class="cancel">Cancel</a> or 
		<%= form.submit "Add new order", :class => "update" %>
	</p>

<% end %>