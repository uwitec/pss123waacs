<!-- Scaffolding generated by Casein v.2.0.6 -->

<h2>Add a new region</h2>

<% form_for :region, @region, :url => casein_regions_path do |form| %>
	
	<%= render :partial => 'form', :locals => {:form => form, :region => @region} %>
	
	<p class="submits">
		<a href="<%= url_for casein_regions_path %>" class="cancel">Cancel</a> or 
		<%= form.submit "Add new region", :class => "update" %>
	</p>

<% end %>