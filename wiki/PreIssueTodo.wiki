#summary チケット発行前のタスク覚書
= WAACS Birdfarm =
 * 営業マン用のアカウントを作る。営業マン権限を用意。現在、Admin 、User
    * 営業マンの注文登録と通常の登録を別扱いにする。
 * memo テストユーザ worker/test123
 * ユーザでの所属倉庫変更は、制限する。
 * ユーザの権限変更をプロファイルで変更するには。
 * ~~ Edi.receive_order 注文受信時の処理 の最終化。~~
 * ~~ Picking Plan.allocate_total, single の最終化。~~
 * ~~ show_xx_list のEDIファイル出力とPdfファイル出力。 ~~
 * 在庫照会での受払情報表示。
    * どのオーダがどこに引き当てられているかをみたい。
 * ~~ ピッキング計画の画面デザイン 最終化。 ~~
 * pdf 在庫帳
    * 不足在庫、補充必要在庫 一覧の別出力
 * ~~ pdf ピッキングリスト （トータル、シングル） ~~
 * ~~ システム管理のトップメニューの文字の大きさを整える。 ~~

 * ~~ total picking で、zzz をfeeding list に反映させるか、どうか ~~
 * ~~ work_no マスタあるいは、そのためのクラス ~~
 * ~~ Edi Fileのcampとの取扱の違いについて整理 ~~
 * ~~ リポート内容の調整 ~~
 * memo Edi File クラス. tagを持ち、ファイルのコントロールを行なう。
 * ~~ 定数 Col配列を、メソッド内で使う。リファクタリング。 ~~
 * allocate_statusの説明記述
 * ~~ panel用のcss highlightが上手くいかない ~~
 * 商品優先引当ロジック
 * 在庫が無いオーダの例も用意する

 * ~~ 本引当のデザイン ~~
 * ~~ wcampとの連携のデザインについて、最終化と実装。 ~~
 * EOS オーダ処理についてのロジック記述。
 * delayed_jobによるバックグラウンドジョブの実行。
 * 仮置ロケーションの設定と記載。

 * max_num , min_num の入力ミスを防ぐためのvalidationをつける

 * pdfトータルピッキングリスト リストの並べ方、集計、欠品の表示順の制御

= WAACS Camp = 
 * エラー処理、エラー表示
 * 出力ファイル行数の表示
 * 検品済表示、完了ははずす。管理者の場合は、表示。
 * 監視画面に入荷一覧も
 