<!-- Scaffolding generated by Casein v.2.0.6 -->

<h2>Add a new number master</h2>

<% form_for :number_master, @number_master, :url => casein_number_masters_path do |form| %>
	
	<%= render :partial => 'form', :locals => {:form => form, :number_master => @number_master} %>
	
	<p class="submits">
		<a href="<%= url_for casein_number_masters_path %>" class="cancel">Cancel</a> or 
		<%= form.submit "Add new number master", :class => "update" %>
	</p>

<% end %>