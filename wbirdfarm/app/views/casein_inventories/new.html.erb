<!-- Scaffolding generated by Casein v.2.0.6 -->

<h2>Add a new inventory</h2>

<% form_for :inventory, @inventory, :url => casein_inventories_path do |form| %>
	
	<%= render :partial => 'form', :locals => {:form => form, :inventory => @inventory} %>
	
	<p class="submits">
		<a href="<%= url_for casein_inventories_path %>" class="cancel">Cancel</a> or 
		<%= form.submit "Add new inventory", :class => "update" %>
	</p>

<% end %>